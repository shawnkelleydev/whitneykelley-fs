import { Link } from "react-router-dom";

const Logo = (props) => {
  return (
    <div className="Logo">
      <Link to="/">
        <h2>Whitney Kelley</h2>
      </Link>
    </div>
  );
};

export default Logo;
